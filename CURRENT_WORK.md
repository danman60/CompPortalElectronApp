# Current Work — CompSyncElectronApp

**Status: 2026-05-22 13:31 EDT — /wrap-up. Post-show (UDC Cobourg ended 05-17). Tree CONSOLIDATED + clean. Branch `feat/ui-redesign-pass1` @ `c8a0932`, pushed, up-to-date with upstream, working tree clean (only gitignored artifacts remain).**

## Last session summary
Post-Cobourg cleanup + deliverables. Built/proven fixes (isolated, NOT yet integrated/swapped), consolidated the working tree into a clean commit, delivered a marketing screenshot + MKV copies. Live DART app unchanged throughout (ffrevert EF44C08F); nothing swapped.

## What changed / shipped this session
- **`c8a0932`** consolidation commit (pushed): 48 modified source files (renderer redesign, recording stop≥15s auto-advance, ffmpeg/jobQueue/upload/wifiDisplay/pipelineHealth, debug capture-renderer route, streamdeck udc-stinger) + new proof harnesses, docs/plans handoffs, F1F2/B1B2 `.patch` files, udc-bug assets, scripts. Extended `.gitignore` to exclude runtime state, stray root Vite bundles, `staging/` asars, `.claude/` agent worktrees, `test-results/`, telegram uploads, `*.bak`.
- **Screenshot delivered** (DM msg 4025): `/tmp/cse-midshow-shot.png` (1920×1080) — rendered from the **live DART build EF44C08F** (pulled read-only to `/tmp/livesnap.asar`), real `VerticalMeters` left of RECORD fed via ws://localhost:9877 audioLevels, 56 TEST2026 routines, session breaks, no disk/SD banners. Caveat: RECORD button shows dark-red *ready* (glow keys off live OBS); the LIVE highlighted table row is the real record indicator.
- **MKV copy**: `18-00-52_26-05-16.mkv` (2.77GB) + `14-59-39_26-05-16.mkv` (145MB) copied (no remux, trailing space stripped) ASTEROID→FIRMAMENT `C:\Users\danie\Desktop\`, byte-verified.

## Built + proven, NOT integrated (isolated branches + committed patches)
- **F1+F2 durable media-completion** (`fix/durable-media-completion`; patch `docs/plans/2026-05-17-durable-media-completion-F1F2.patch`). 16/16.
- **B1+B2 4700-counter/queue fix + F1F2** (`fix/post-cobourg-batch`; patch `docs/plans/2026-05-19-post-cobourg-B1B2.patch`). 14/14 + 16/16. Close-dialog count 1509→6 in proof.
- ⚠ Both patch-bases = committed `7a0baf8`; folding into the redesign tree needs a deliberate 3-way merge (will conflict in upload.ts/state.ts/types.ts/jobQueue.ts/index.ts). Re-run both harnesses on the merged tree before any build. Never bundle with an encode-deadlock asar.

## The "4700 pending" investigation (RESOLVED — root cause, evidence-based)
Close-dialog "uploads pending" = raw `jobQueue.getPending('upload')+getRunning('upload')` count (`src/main/index.ts:217`). Inflated by: thumbnails enqueued as separate jobs, duplicate re-enqueues of already-uploaded files (fail "already exists"), order-A gated remaining-photo tier, and never-pruned terminal failed/quarantined records. DB proved zero media lost (544/545 published). Fix = B1 (distinct-undelivered count + enqueue dedupe + terminal prune) — built, in `fix/post-cobourg-batch`, not integrated.

## Re-scope of the post-Cobourg fix list (staleness sweep — most items were already done in-tree)
Plan: `docs/plans/2026-05-19-post-cobourg-fix-batch.md`. STALE/already-implemented (shipped via this commit): slow-zoom CUT+FADE-4s, photo-tier order A, day-scoped RoutineTable, BATCH alerts, CSE archive-media row action, import-pill pulse, audio-meter 10Hz throttle.

## Open / NOT done (operator's call — do NOT auto-start)
1. **D1 R222/R223 photo cleanup — STALE PREMISE, do NOT run the original UPDATE.** DB now: R222 pkg `c40d0848`=84 photos (cap 05-15 16:19:59–16:25:53 EDT); R223 pkg `d9961024`=**43 photos already** (NOT 0; cap 12:23:31–12:25:53 EDT); both published. Windows don't match the "split @20:23:02Z" premise. Ask operator for the correct end-state before any DB write.
2. **B4** activity-log status tabs (Queued/Encoding/Uploading/Uploaded board) — genuinely not built.
3. **B5-CompPortal** clear-media endpoint (CSE-side archive-media already in tree; needs the server endpoint that sets deleted_at + nulls video fields).
4. **B8** R2 archive-on-overwrite (CompPortal `src/lib/r2.ts` signGuardedUpload overwrites in place; `copyInR2` exists but unused; ~30-45min, Vercel deploy).
5. **C2** swap the staged 82C34FC0 build, or leave DART on ffrevert — encode deadlock self-resolved live, so this is preventive only. Staged at `C:\CompSync-staging\app.asar.new`; 6825923C preserved as `.bak`.
6. **Fold F1+F2 / B1+B2** into the tree when ready (3-way merge per above).

## Hard rules still in force
Never close/kill/swap on DART without explicit operator go (operator owns app close+relaunch); each asar swap its own gated action; DB/R2/CompPortal writes need explicit go; machine_logs (Supabase COMPSYNC, ts UTC→EDT) not ssh for DART logs; ASTEROID/DART PowerShell over ssh MUST be a `.ps1 -File` (inline `-Command` quoting mangles — burned 2× this session); strip `\r` from PowerShell stdout before bash compares; PowerShell `Write-Output` per-file in `while read` loops needs `ssh -n`/`</dev/null` (stdin consumption).

---

**Status: 2026-05-17 13:23 EDT — Plain-Stop auto-advance woven into the staged ASAR (RESTAGED, NOT swapped). Operator directive: a plain Stop (NOT Next) of a take >=15s advances routine SELECTION to next routine (no recording started); <15s stays. Single-file change `src/main/services/recording.ts` (handleRecordingStopped): captures `navInitiatedStop = pendingStopProcessing !== null` at entry (next()/nextFull() route through stopRecordingAndWait→barrier; plain Stop/hotkey/OBS-auto-stop don't), sets `advanceAfterStop = !navInitiatedStop && durationSec>=15` after the recording.stopped emit, calls `state.advanceToNext()` in finally before broadcast. No double-advance (nav stops suppressed). sub-10s discard path returns earlier; 10–14s stays. OBS auto-stop intentionally treated as plain Stop (operator: "OBS shouldn't matter"). electron-vite + electron-builder exit 0. Combined asar rebuilt from shared tree = Codex 6825923C content (in-asar verified: freeWifiDisplayPorts/nudgeRoutineEncode/resumeRecordedRoutines) + this change + Header.tsx/ShowControlRail.tsx edits already in the shared tree @12:37 EDT (post Codex 12:16 build). Encoder SAFE in-asar: h264_cuvid=0, hwaccel cuda=0, hwaccel_output_format=0. NEW staged `C:\CompSync-staging\app.asar.new` = **sha `82C34FC08A9191DBAA3E19EA33447AC8A25B2DBB02781873D63E3105E3F23D8A`, 132,578,365 bytes**, LastWriteTime 2026-05-17 13:23:02 EDT, byte-identical local↔DART verified. Codex's prior 6825923C (132,578,089) PRESERVED at `C:\CompSync-staging\app.asar.6825923C.bak` (intact, rollback target). Live DART `app.asar` UNCHANGED (ffrevert EF44C08F, 132,569,626). NOT swapped, NOT committed, Program Files + running app untouched. Encode-deadlock swap remains operator-gated + Codex-coordinated; staged-sha verification expectation is now 82C34FC0…, not 6825923C (Codex notified via INBOX). feat/ui-redesign-pass1.**

**Status: 2026-05-17 13:02 EDT — /fresh RESTART (long live-incident session, context rot — many wrong theories burned, operator called fresh). UDC Cobourg 2026 LIVE on DART. Codex is co-working this and maintains the authoritative status block below (12:18). READ THIS + the 12:18 block + .claude-crash-transcript.md first.**

**LIVE INCIDENT STILL OPEN — video encode deadlock on DART:**
- Routine **539 "KING KUNTA"** stuck in `encoding` state (its ffmpeg was killed by the 10:25 asar swap-close). App derives `ffmpeg-busy` from a routine in `encoding` → startup encode auto-resume skipped (`Auto-resume skipped: ffmpeg-busy`, earlier `sd-import-in-progress`) → no ffmpeg ever spawns → 539 never clears → self-perpetuating deadlock. Zero real ffmpeg since 10:23:35. ~26+ routines need encode; recording continued (~547+). Operator levers (relaunch, kick, nudge, in-app clear) all bounce — 539 flips `encoded→encoding` right back.
- Full diagnosis + failed-attempts log: `docs/plans/2026-05-17-encode-deadlock-handoff-for-codex.md`.
- **DART live = ffrevert `EF44C08F7EBAF372…` (132,569,626, swapped 10:52:02) — STILL HAS THE DEADLOCK.** Rollback backup on DART: `app.asar.bak.20260517-ffrevert-pre` (frozen combined FF281CF0); `app.asar.bak.20260517-combined` (089283, pre-swap, encoded fine until 10:23 but same resume gating).

**Consolidated pre-swap ASAR (Codex, authoritative — per 12:18 block below): `C:\CompSync-staging\app.asar.new` = 132,578,089 / sha `6825923C4A55D93F…` (staged 12:18:12).** Contains: Encode PIPE stage + Kick/Nudge fixes (resumeRecordedRoutines resets stale running encode jobs — the actual deadlock fix), SD safe-eject pill restore, ffrevert encoder (no hwaccel/cuvid), orphan-resume, photo-tier[A], exif-bookmark, archive-media, chat-highlight, upload-speed, stinger, **wifi-display self-heal** (freeWifiDisplayPorts in wifiDisplay.ts — taskkills stale wifi-display-server before spawn so the port-5000 crash loop can't recur). NOT swapped. Earlier superseded staged builds (chatfix 339c2376, kickfix 63d8065c, wifidisplayfix b4880aca, 86ae617c) are stale — `app.asar.new` 6825923C is the one.

**RESOLVED THIS SESSION (live, confirmed):**
- **Stream Deck plugin fixed-forward + installed.** Root cause of all-green-blank keys: Codex's staged `streamdeck-plugin` folder was missing `node_modules/` (Elgato Node runtime) → plugin couldn't execute. Fix: rebuilt complete folder locally (rollup), node_modules + stinger present, installed to `C:\Users\User\AppData\Roaming\Elgato\StreamDeck\Plugins\com.compsync.streamdeck.sdPlugin`. Backups kept: `.bak.20260517-prestinger` (pre-stinger working), `.broken-20260517-incomplete` (the bad one). **Operator must launch Stream Deck from DART desktop** (SSH-launch = Session 0, no GUI) to load it.
- **Tablet/wifi-display restored.** Was a crash-loop: stale `wifi-display-server.exe` PID 22712 (11:34:57) squatting UDP 5000/5001 → every respawn `os error 10048` → exit 1 → loop. Killed PID 22712 (+orphan ffmpeg already dead); supervisor auto-rebound (PID 5328, 11:52:23); tablet rendering + touch confirmed working. The wifi-display self-heal (in the staged 6825923C build) prevents recurrence.

**NEXT SESSION — DO NOT auto-start. Operator + Codex own the encode-deadlock swap.** When operator gives go: standard gated cutover of `app.asar.new` (6825923C) — app CLOSED, backup live → `app.asar.bak.20260517-<tag>`, Move `app.asar.new`→live, verify sha `6825923C`, operator relaunches. After relaunch verify (machine_logs, supabase-COMPSYNC, col `ts` UTC→EDT): `Resume recorded: queued N` fires, ffmpeg `frame=` advancing, 539 clears, backlog drains. Coordinate with Codex (it built 6825923C) before staging/swapping anything to avoid divergence. Hard rules unchanged: never close/kill/restart DART apps (operator owns), each swap its own gated action, no DB/R2/CompPortal writes, machine_logs not ssh for logs, builds in subagents, GitNexus MCP DOWN.

**Status: 2026-05-17 12:18 EDT — Encode PIPE/Nudge + restored SD safe-eject pill ASAR staged to DART, NOT swapped.** Header `PIPE` popover now includes an `Encode` stage (`src/shared/types.ts`, `src/main/services/pipelineHealth.ts`, `src/renderer/components/PipelineHealthChip.tsx`) with pending+running encode count, 5/10 min stale health, and activity bumps on encode enqueue/frame progress/resume. `Kick All Stages` already routes through `JOB_QUEUE_KICK`; backend now makes that path actually kick encoding by calling `resumeRecordedRoutines()` plus `resumeEncoding()` where `resumeEncoding()` always calls `processNext()`, not only when paused. `resumeRecordedRoutines()` also resets stale running encode jobs to pending when no FFmpeg process / encode loop is alive. Row `Nudge` for recorded/queued/encoding routines now goes through `ffmpegService.nudgeRoutineEncode()` via `FFMPEG_ENCODE`, which reuses existing pending/running encode jobs, resets stale running jobs, queues missing jobs, clears stale `encoding` display back to `queued`, and starts the encode loop. Follow-up top-row regression fix: the SD import complete / "Safe to remove" pill still existed, but was inside `.topband-brand` capped to 300px with `overflow:hidden`; moved it into its own `.topband-import-safe` top-row slot that is zero-width when empty and capped at 360px when active, so the safe-eject notification is visible without swallowing PIPE/queue. Encoder remains reverted/safe: packaged ASAR has no `-hwaccel cuda`, no `h264_cuvid`, no `cuda hwaccel decode`. Verification: `npm run build` PASS; `npx electron-builder --win --dir` PASS. Local ASAR `release/win-unpacked/resources/app.asar` size **132,578,089 bytes**, sha256 **`6825923c4a55d93f7472f23c4830c1823c6e5756d8c9f4befb97f2ef3f2a0c0f`**, mtime 2026-05-17 12:16:56 EDT. DART staged `C:\CompSync-staging\app.asar.new` size **132,578,089 bytes**, sha256 **`6825923C4A55D93F7472F23C4830C1823C6E5756D8C9F4BEFB97F2EF3F2A0C0F`**, LastWriteTime **2026-05-17 12:18:12 EDT**. DART live remains unchanged: `C:\Program Files\CompSync Media\resources\app.asar` size **132,569,626 bytes**, sha256 **`EF44C08F7EBAF372482F0061173C8BB41806D55DE381F15F6728EB97CC933569`**, LastWriteTime **2026-05-17 10:52:02 EDT**. Branch is even with upstream (`git rev-list --left-right --count HEAD...@{u}` = `0 0`), so rebase was a no-op; dirty worktree prevented a working-tree rewrite anyway. Not swapped, not restarted, not committed.

**Status: 2026-05-17 09:56 EDT — Chat-highlight + ARCHIVE MEDIA combined ASAR STAGED to DART, NOT swapped, NOT committed.** Codex's uncommitted chat-highlight in `OverlayControls.tsx`/`overlay-controls.css` typecheck-verified (0 genuine errors — all `window.api unknown` project-wide structural noise, identical across all files; electron-vite build exit 0). New ARCHIVE MEDIA row action implemented scope-A CSE-local only: `recording.ts` new exported `archiveRoutineMedia()` reuses the EXACT `_archive/v{N}/` versioned-rename mechanism from `archiveExistingFiles()` (files moved via `fs.promises.rename`, never deleted; prior Take rows get `state.setTakeArchived`), then resets the routine via the `scratchRoutine`-pattern field clear but to `'pending'` (pre-record). New IPC channel `routine:archive-media` (types.ts + ipc.ts handler + preload `routineArchiveMedia`). New "Archive Media" ⋯ row-menu item in `RoutineTable.tsx` with `window.confirm` (no audible alert, no recording-blocking modal). Combined build: `npx electron-vite build` + `npx electron-builder --win --dir` → `release/win-unpacked/resources/app.asar` **132,572,045 bytes**, sha256 `ff281cf04c3f1d829784da6ca7391f409d793d726fa9cdbc3bc704fa68d117b1`, mtime 2026-05-17 09:51:38 EDT. All accumulated tree work verified intact in-asar (h264_cuvid×4, resumableStatuses×2, sdFilenameBookmark×17, bytesPerSecond×2). Headless CSE-local test (Xvfb + CDP, synthetic TEST2026 fixture, no OBS/network/DB) ALL PASS: chat-highlight CSS present, ⋯ menu shows "Archive Media", archive moves-not-deletes (2MB mkv byte-identical sha in `_archive/v1/`, gone from top level), routine reset uploaded→pending with all media refs cleared. Screenshots `/tmp/archive-media-test/`. Staged DART `C:\CompSync-staging\app.asar.new` 132,572,045 bytes sha `FF281CF04C3F1D829784DA6CA7391F409D793D726FA9CDBC3BC704FA68D117B1` — BYTE-IDENTICAL to local. Live `C:\Program Files\CompSync Media\resources\app.asar` 132,558,065 bytes LastWriteTime 2026-05-16 20:24:59 EDT UNCHANGED. NO live swap, NO app restart, NOT committed. Scope B (CompPortal/uploaded-copy archive) deferred by design.



**Status: 2026-05-17 09:03 EDT — Fresh combined ASAR staged to DART, not swapped.** Rebuilt Windows dir package from current local tree (`npm run build && npx electron-builder --win --dir`) after upload-speed UI + Stream Deck UDC Stinger additions. Local `release/win-unpacked/resources/app.asar` size 132,564,265 bytes, sha256 `d6e7daa6a9baa759189eeef50758a48ec60f9ae2e55c8044f02199b66a681a8f`. Local ASAR strings confirm `h264_cuvid`, `bytesPerSecond`, `upload-rate`, and `setUdcStingerTransition`. Copied to DART staging only: `C:\CompSync-staging\app.asar.new` size 132,564,265 bytes, sha256 `D6E7DAA6A9BAA759189EEEF50758A48EC60F9AE2E55C8044F02199B66A681A8F`, LastWriteTime 2026-05-17 09:02:38 EDT. Also staged matching Stream Deck extraResource folder at `C:\CompSync-staging\streamdeck-plugin` (22 files, LastWriteTime 2026-05-17 09:02:43 EDT) because the Stream Deck plugin is not stored inside `app.asar`; staged manifest/plugin contain `com.compsync.streamdeck.udc-stinger` and `setUdcStingerTransition`. DART live remains unchanged: `C:\Program Files\CompSync Media\resources\app.asar` size 132,558,065 bytes, sha256 `089283287720AD55D818F20F0A82D23BF84359AAA975F5DA78951ACE2F574B72`, LastWriteTime 2026-05-16 20:24:59 EDT. No process restart, no live swap.

**Status: 2026-05-17 08:53 EDT — Upload speed in system stats + UDC Stinger Stream Deck action built locally, not live.** Upload progress now carries optional `bytesUploaded`, `bytesTotal`, and `bytesPerSecond` from both child-process and main-process upload paths. `Header.tsx` renders active upload status/speed inside the existing system-stats row as `UP filesDone/filesTotal rate` (example verified: `UP 2/7 4.6MB/s`) without removing CPU/RAM/Disk/OBS. Local 1920x1080 screenshot captured from `out/main/index.js`; final measured row with active queue + upload speed: `.topband-meta` right=1522 inside innerWidth 1536, `.topband-system` right=716, `.topband-meta-stats` right=1008, `.topband-actions` right=1522. Telegram DM sent message_id 3795 full screenshot and 3796 crop. Stream Deck plugin adds `com.compsync.streamdeck.udc-stinger`, registered in `plugin.ts`, listed in `manifest.json`, built into bundled `bin/plugin.js`; WebSocket action `setUdcStingerTransition` sets OBS transition to exact name `UDC Stinger`. Existing OBS `SceneTransitionEnded` auto-revert still handles snapping non-Cut transitions back to Cut after they fire. `npm run build` PASS and `streamdeck-plugin npm run build` PASS. No DART staging/swap, no live push.

**Status: 2026-05-17 08:39 EDT — Local UI top-row 1080 refinement built, screenshot-sent, not live.** `src/renderer/components/Header.tsx` moved encode/upload pause controls out of the overlapping action-bar footer into a compact queue-control cluster beside the pipeline chip, preserving the same `uploadStart/uploadStop` and `ffmpegResume/ffmpegPause` actions. `src/renderer/styles/header.css` pins the brand text to one line, tightens queue-control labels/counts, preserves a non-shrinking CPU/RAM/Disk/OBS system-stats slot, bounds the DONE/PIX/REC strip, and keeps Overlay/Settings as fixed icon-size top-row buttons. `npm run build` PASS. Local Electron screenshot at 1920x1080 captured from `out/main/index.js` with DPR 1.25; final active-queue + system-stats measured row: `.topband-meta` x=14 w=1508 right=1522, `.topband-brand` w=300 right=314, `.queue-control-cluster` x=188 w=106 right=294, `.topband-system` x=322 w=316 right=638, `.topband-meta-stats` x=650 w=280 right=930, `.topband-actions` x=1182 w=340 right=1522, all inside innerWidth 1536. Telegram DM sent message_id 3792 full screenshot and 3793 top-row crop after the system-stats correction. No DART staging/swap, no live push.

**Status: 2026-05-16 22:32 EDT — Full-GPU decode/encode ASAR STAGED to DART, not swapped.** `src/main/services/ffmpeg.ts` smart encode now tries explicit `h264_cuvid` decode first for NVENC jobs, uses CUVID `-resize` for 480p/720p judge video, falls back to the current live `-hwaccel cuda` NVENC path, then falls back to CPU `libx264` only if both GPU paths fail. Primary FIRMAMENT artifact `/mnt/firmament/ff5test/result.txt` proves exact gyan 5.0.1 V3 `h264_cuvid -> h264_nvenc` EXIT=0, output 9,322,482 bytes. SpyBalloon local proof against real OBS MKV: 1080p `h264_cuvid -> h264_nvenc` EXIT=0, 8,971,099 bytes; 480p CUVID `-resize 854x480` EXIT=0, 543,260 bytes; 720p CUVID `-resize 1280x720` EXIT=0, 2,092,846 bytes. `npm run build` PASS. Local packaged ASAR `release/win-unpacked/resources/app.asar` size 132,560,171 bytes, sha256 `973ee64da71213bc06e65e2cd46d9cabb31f57987dbd8d24dddf30679ca26db5`, extracted ASAR contains `h264_cuvid` x4. Staged on DART at `C:\CompSync-staging\app.asar.new`, size 132,560,171 bytes, sha256 `973EE64DA71213BC06E65E2CD46D9CABB31F57987DBD8D24DDDF30679CA26DB5`, LastWriteTime 2026-05-16 22:32:05 EDT. DART live ASAR remains unchanged: size 132,558,065 bytes, sha256 `089283287720AD55D818F20F0A82D23BF84359AAA975F5DA78951ACE2F574B72`, LastWriteTime 2026-05-16 20:24:59 EDT. NOT swapped, NOT committed. Remaining caveat: SpyBalloon is Linux ffmpeg 6.1.1 evidence; FIRMAMENT gyan 5.0.1 primary artifact proves the 1080p CUVID path, but the new CUVID `-resize` judge path still needs FIRMAMENT proof when GPU is not occupied by Topaz.

**Status: 2026-05-16 ~20:15 EDT — EXIF-BOOKMARK subagent COMPLETE (the `aec90245c63805cf0` deliverable). (Subagent mis-stamped this "2026-05-17 00:15"; it confused UTC for EDT — actual Eastern ≈ 20:15 on 2026-05-16.)**

- ADDITIVE per-(volumeSerial, DCIM subfolder) PRE-READ filename bookmark added IN FRONT OF the existing EXIF cursor + DB dedup + ±30s window gate. photos.ts (~190 lines, fail-safe: known-serial + existing-bookmark + !previewOnly + !allowlist gated; boundary re-read band default 5; rollover→full-read; unknown card→no pre-skip) + state.ts (new persisted `sdFilenameBookmarks` map + forward-only API). Orphan fix (ffmpeg.ts `resumableStatuses`) + photo-tier (jobQueue.ts/upload.ts, order B left UNTOUCHED — operator A/B still open) VERIFIED INTACT and present in the rebuilt asar.
- Combined local asar (⚠ CORRECTED — the subagent's own mid-run figure 132,557,834 / `1b3f5fc…` was STALE; a final rebuild followed the harness-fidelity fixes). **DISK-VERIFIED AUTHORITATIVE 2026-05-16 20:16 EDT (`sha256sum` + `ls` on the actual file):** size **132,558,065 bytes**, sha256 **`089283287720ad55d818f20f0a82d23bf84359aaa975f5da78951ace2f574b72`** at `release/win-unpacked/resources/app.asar`. All 3 fixes confirmed in-asar. LOCAL ONLY — NOT scp'd to DART, NOT committed. (Prior pre-exif combined was 132,551,676.) **This asar is ORDER B — the operator-locked B→A photo-tier flip is STILL PENDING (subagent explicitly left order B). A separate consolidation pass MUST apply B→A + rebuild + re-prove before any swap, because operator decided A.**
- All 3 fixes confirmed inside the asar (@electron/asar extract): `resumableStatuses`×2, `hasOutstandingVideoWork`×3, `sdFilenameBookmarkKey`×6 / `fn-bookmark`×6.
- electron-vite build = exit 0 (the real project compiler). Bare `npx tsc -p tsconfig.node.json` has 134 PRE-EXISTING project-wide errors in untouched files; in photos.ts/state.ts only 17 TS2802 downlevelIteration-target noise (16 at HEAD, +1 from my new for-of) — ZERO genuine type errors, no new error class. esbuild does not emit these.
- Proof harness `tests/exif-bookmark/` (real photos.ts/state.ts esbuild-bundled, only electron+volumeSerial+recording boundaries shimmed, real EXIF JPEGs). Dedup map `tests/exif-bookmark/CURRENT-DEDUP-MAP.md`. Photo-tier order still **B** (operator A/B open — separate B→A consolidation pass below NOT done by this subagent).

---

**Status: 2026-05-16 19:40 EDT — /fresh RESTART (operator: "you're getting dumb"; session long, looping on pill UX). UDC Cobourg 2026 LIVE on DART. READ THIS BLOCK FIRST.**

**In flight RIGHT NOW:**
- **Background subagent `aec90245c63805cf0` is RUNNING** — building the per-DCIM-subfolder PRE-READ EXIF bookmark, folded with orphan fix + photo-tier into ONE local app.asar, with a "zero new photos dropped" proof harness. It auto-notifies on completion. The fresh session must NOT duplicate its work (photos.ts/state.ts/jobQueue.ts/upload.ts/build). When it completes: review its dedup-map + before/after proof + report to operator.
- **PHOTO-TIER ORDER = A, LOCKED by operator 19:36** ("a small amount of new photos always jump the queue"): in jobQueue.ts getNext('upload') the priority-photo (1/3, photoTier!=='remaining'/missing→priority) tier must be selected BEFORE the videoJobs block. Final order: 1/3-new-photos → video(encode/upload) → remaining-photos(gated behind encode+video). The current built combined asar (132,551,676 / sha 68b4d760) is order **B** (videos on top) — needs B→A flip. SendMessage to the running agent is NOT available, so AFTER the exif agent finishes: ONE consolidation pass = apply B→A + rebuild + re-prove the single combined asar (orphan + photo-tier[A] + exif-bookmark). A MUST land in final build.
- **Pill "renders as FROZEN":** NOT a render bug, no literal FROZEN text. It's the import-pill at a stuck 0/xxx + App.tsx:127 stalled alert because the scan crawls (post-read EXIF cursor reads all ~1000 files + flaky reader ENOENT). Operator (19:39): "I just want it to stop freezing" + "no extra text on the pill." Resolution = the exif pre-read bookmark (in flight) makes the scan fast → pill stops sitting frozen. NO pill UI/text change. (Optional, only if operator later asks: pure no-text `.import-pill-dot` pulse — default = do nothing to the pill.)

**Live pipeline (last good read ~19:11):** encode gap ~37, recording active, 0 NVENC failures, encoder healthy (GPU fix holding). Photo uploads flowing (334/8min @19:24, 0 failures, stuck SD-import lock cleared). A ScheduleWakeup safety-watch loop is/was armed (~30-min cadence) — re-arm it if continuing to monitor; ping operator only on genuine stall (verify FFmpeg `frame=` first), NVENC failures, or gap +10.

**Built + proven, LOCAL only, NOT staged to DART, NOT committed (feat/ui-redesign-pass1):**
- Orphan fix (ffmpeg.ts `resumableStatuses={recorded,queued,encoding}`) — resumes encode queue on restart so no manual kick.
- Photo-tier priority (jobQueue.ts/upload.ts) — currently order B; must become A.
- Combined asar pre-exif: 132,551,676 / sha256 `68b4d7604d0fe182ca12109f56f85fc286414fc1ac42b01871b116422da9934c`.
- GPU-decode fix already LIVE on DART since 13:11 (132,549,034 / sha cef26356) — confirmed working, regression dead.

**Pending OPERATOR decisions (do NOT act without explicit go):** (1) swap window for the combined fixed build — swapping self-heals the orphan dormancy (no manual kick); restarting the *current* live build orphans the ~37 backlog (needs manual kick). (2) app-restart call for SD/import (clears stuck import lock; card/reader ENOENT is hardware — restart won't fix a flaky reader). NO stage/swap/commit/DART action without explicit operator go. The /fresh new session must WAIT for instructions, NOT auto-start.

---

**Status: 2026-05-16 12:45 EDT (POST-CRASH RECOVERY — SpyBalloon power outage ~11:45 EDT killed the session mid-answer; context rebuilt from .claude-crash-transcript.md + transcripts/2026-05-16). UDC Cobourg 2026 Day 2 LIVE on DART. ACTIVE INCIDENT: GPU-encode regression is LIVE and NOT yet fixed (operator said "Don't swap show is live").**

**What happened:** Earlier this session a ffmpeg GPU-decode change shipped that added `-hwaccel cuda -hwaccel_output_format cuda` to the encode command. That strict zero-copy form **fails (exit 1) on DART's gyan ffmpeg 5.0.1** (`Function not implemented` / `No decoder surfaces left`), so every routine falls back to **libx264 software encode**. Effect on the live box: ffmpeg pinned belownormal, ~0.88x realtime, ~4.7 min/routine.

**The fix — built, FIRMAMENT-validated, STAGED, NOT SWAPPED:** Drop `-hwaccel_output_format cuda`, keep `-hwaccel cuda` (2-area change in `ffmpeg.ts` `hwDecodeArgs`/`judgeOnGpu` region; logo port kept). FIRMAMENT test (Windows + RTX 4090 + exact gyan 5.0.1 + real OBS mkv): V1 strict form = EXIT 1 (reproduced DART failure), **V2 `-hwaccel cuda` only = EXIT 0** (valid output, nvenc, GPU decode active), V3 cuvid = EXIT 0 (backup), V4 CPU-decode→nvenc = EXIT 0 (control). Cause is a deterministic flag/filter incompatibility, NOT DART GPU/driver/OBS contention. Staged on DART: `app.asar.new` = **132,549,034**, sha256 `cef26356…`, 3 sprites in place, logo port intact. libx264 fallback code deliberately unchanged (correct safety net) — it stops firing because the trigger (exit 1) is gone. One honest open caveat: FIRMAMENT had no OBS; a *separate* nvenc-session-contention failure (different error signature: `OpenEncodeSessionEx failed`/`no NVENC capable device`) is not disproven — first post-swap encode log line settles it instantly.

**Live backlog at recovery (primary source, machine_logs, 12:41 EDT):** show RECORDING Entry #383 "A MES ETOILES" (Pos 289); encoder working on R350. **Gap ≈ 33 routines behind, GROWING** (was ~30 an hour ago). Encode (~4.7 min/routine) is slower than live recording cadence (~2.5–3.5 min/routine) → backlog widens until the fix is swapped; cannot self-recover on the current build.

**RESOLVED 2026-05-16 ~13:08–14:36 EDT — GPU FIX SWAPPED & CONFIRMED WORKING LIVE.** Operator gave go at 13:07 ("Ok swap"), closed the app at 13:08. Swap done via two single-line ssh Move-Items: live `app.asar` (132,549,469, the regressed build) → `app.asar.bak.20260516-1308-gpureg`; staged `app.asar.new` → live `app.asar`. Verified live = **132,549,034**, sha256 `CEF26356EF8AABD545E29113987192029035367A0E0E66A9CA5D2412242365BB` ✓ exact match to FIRMAMENT-validated build. Operator relaunched at **13:11:00** (Overlay server + FFmpeg 5.0.1 markers). At 13:11:12 startup recovery reset 45 orphaned uploading→uploaded + 21→encoded. **Live proof at 14:34–14:36:** R355 "THAT GURL" encoded fully on the fix — step 1 `h264_nvenc, cuda hwaccel decode` (cmd = `-threads 16 -hwaccel cuda -y -i …` — NO `-hwaccel_output_format cuda`), step 1b judge video now `h264_nvenc` (was libx264), `encoding → encoded` in ~2 min, NEXT routine started 1 sec later. Zero "NVENC failed / falling back to libx264" anywhere post-relaunch. nvenc succeeds WHILE OBS live-records simultaneously → OBS-NVENC-contention caveat also disproven by live evidence. Encode is serial back-to-back at ~2 min/routine (nvenc) which beats the ~2.5–3.5 min recording cadence → the regression-era backlog now DRAINS instead of grows (recording #392, encoder cleared #355 at 14:36 → ~37 nominal behind, minus the 21 force-reset, shrinking over the session). The transient "97% CPU, no recording" earlier was the morning's photo-upload + thumbnail backlog catching up (child upload procs + webp + Defender) + overlay obs-browser-page baseline — not the regression (only 1 ffmpeg, not the libx264 storm). See [[reference_dart_ffmpeg_hwaccel_flag]].

**RESTART-ORPHAN ENCODE BUG — FIX BUILT + PROVEN (NOT STAGED, NOT COMMITTED) 2026-05-16 ~15:49 EDT.** Root cause: `resumeRecordedRoutines()` (ffmpeg.ts:1268) filtered `status !== 'recorded'` — only `recorded` routines resumed on boot; `queued`/`encoding` routines (the normal pre-encode state — see live "recorded → queued" transitions) fell through every boot-recovery path (index.ts:587 reconcile only handles `uploading`). In-memory job queue dies on restart (index.ts:577), so an asar swap on a deep `queued` backlog orphans the whole queue until a manual kick — exactly today's 80-min idle. Prior resilience work (2026-05-04 `uploading` reconcile, resumeRecordedRoutines `recorded`) was status-specific and never closed the `queued`/`encoding` hole. **Fix (systemic):** `resumableStatuses = {recorded,queued,encoding}`; explicit `state.updateRoutineStatus(id,'queued',{encodeSkipReason:undefined})` before enqueue (verified enqueueJob has NO status side-effect — caller owns the transition, recording.ts:993 pattern; without this a resumed `encoding` routine displays "encoding" forever). Signature `(): number` unchanged → 5 callers (ipc.ts:1364/1418, index.ts:621, wsHub.ts:268/343) unaffected. Pre-existing GPU cuda-hwaccel changes in ffmpeg.ts untouched. **PROOF (before/after harness, identical fixture, primary-source verdict from job-queue.json + `Resume recorded: queued N` log + per-routine status):** RUN A unpatched = only 2 `recorded` resume, 5 `queued`/`encoding` orphaned (reproduces today's failure); RUN B patched = all 7 resume + encoded with no manual kick, `encoded`/`uploading` correctly excluded, no double-enqueue. Opposite outcomes, same fixture. Harness committed at `tests/orphan-resume/` (run-harness.sh / make-fixture.mjs / check-verdict.mjs) — repeatable per-build regression lock. tsc exit 0. Windows `app.asar` = **132,549,225 bytes**, sha256 `c74e1a3024f954dafc4205f18aa4fe52304839fdee5486a968f26a725a0a2293`, LOCAL at `release/win-unpacked/resources/app.asar` — NOT on DART. Faithfulness caveat (disclosed): exercised under Linux Electron on the exact electron-vite bundle electron-builder packs (orphan bug is pure JS state/boot logic, OS/GPU-independent); Windows asar built+hashed but can't run on Linux — same reasoning as the FIRMAMENT GPU validation. **STAGING + SWAP ARE SEPARATE OPERATOR-GATED STEPS — do not scp to DART or swap without explicit go; show is live. NOT committed (feat/ui-redesign-pass1).**

**PHOTO-TIER PRIORITY + ORPHAN FIX — COMBINED BUILD DONE + PROVEN, LOCAL ONLY, ONE OPEN ORDERING DECISION 2026-05-16 ~18:10 EDT.** Operator rule: ~1/3 of each routine's photos top-priority (round-robin), then video/encode/upload, then remaining ~2/3 photos. Implemented: upload.ts tags photo jobs `photoTier:'priority'|'remaining'` at enqueue (`Math.ceil(count/3)` priority, both enqueue paths; missing tier → treated priority, no deadlock); jobQueue.ts getNext('upload') tiers + new exported `hasOutstandingVideoWork()`/`hasPendingRemainingPhotos()`; processLoop deadlock found+fixed (was `if(!job) break` permanent exit → now bounded 3s re-check while remaining-gated). Proof harness `tests/photo-tier/` (real-module esbuild, no logic mocked): RUN A unpatched = 28/28 photos dispatched while encode pending (starvation reproduced); RUN B patched = 0 remaining before video cleared, all 18 released after, 0 busy-spin, legacy untagged=priority. Orphan fix (resumableStatuses, ffmpeg.ts) confirmed still intact in same tree. Combined build LOCAL `release/win-unpacked/resources/app.asar` = **132,551,676 bytes**, sha256 `68b4d7604d0fe182ca12109f56f85fc286414fc1ac42b01871b116422da9934c`, tsc 0 errors, both fixes in asar. NOT staged to DART, NOT committed. **OPEN DECISION (operator):** implemented order is `videoUpload → priorityPhoto(1/3) → [gated]remainingPhoto` — the agent preserved the OLD operator-locked "Burlington 2026-05-01: videos ALWAYS before photos" rule on top. Operator's NEW stated order is `ROUNDROBINPHOTO(1/3) → ENCODE → UPLOADVIDEO → REMAINING` (1/3 photos ABOVE video uploads). Two operator-locked rules in tension — awaiting operator pick: A = move 1/3 photos above video uploads (strict to new order); B = keep videos on top (current build). If A, jobQueue.ts getNext must select priority-photo tier before videoJobs. NO stage/swap without explicit go.**

**PHOTO-TIER ORDER DECIDED = A (operator, 2026-05-16 19:36 EDT).** "A small amount of new photos need to ALWAYS jump the queue" — priority-photo (1/3) tier selected BEFORE videoJobs in jobQueue.ts getNext('upload'). Final order: 1/3-new-photos → video(encode/upload) → remaining-photos(gated behind encode+video). Current built combined asar (132,551,676 / 68b4d760) is order B; needs B→A flip. SendMessage to running exif-bookmark agent (aec90245c63805cf0) NOT available → cannot inject mid-run. PLAN: let exif agent finish (it preserves B), then ONE consolidation pass applies B→A + rebuilds + re-proves the single combined asar (orphan + photo-tier[A] + exif-bookmark). A MUST land in final build.

**OPEN UX ITEM (tracked, 2026-05-16 19:36 EDT):** import/scan pill "renders as FROZEN" — no literal FROZEN string; it's the import-pill showing a non-advancing 0/xxx + App.tsx:127 "Photo import stalled — no new photos in N min" alert, because the scan genuinely crawls (post-read EXIF cursor reads all ~1000 files + flaky reader ENOENT). Same root cause as "scanning too long." Real fix = the in-flight exif pre-read bookmark (fast scan → pill no longer sits at a stuck count → "frozen" appearance gone, NO pill change needed). Operator 2026-05-16 19:39: explicitly does NOT want any additional text on the pill — the earlier "last advanced Ns ago / N of M" idea is REJECTED. Only acceptable pill tweak (optional, operator-gated) = pure no-text animation: existing `.import-pill-dot` pulses while scan is actively progressing. Default = no pill change; rely on the speed fix. Not built; tracked.

**NEXT ACTION = monitor backlog drain over the session (no intervention needed); nothing else gated-but-pending changed.** Do NOT commit (still nothing committed on feat/ui-redesign-pass1; operator has not asked). The 5 OUTSTANDING items below (carry-over B R222/R223, perf #1, clear-media, R2 archive-on-overwrite, commit/push) remain gated and NOT started. Backups on DART now include `app.asar.bak.20260516-1308-gpureg` (132,549,469, the regressed build — rollback target if any nvenc issue appears).

---

**Status: 2026-05-16 07:55 EDT (/fresh handoff — long live-incident session). UDC Cobourg 2026 LIVE on DART. Live `app.asar` = 132,532,621 (backtick-fix build, swapped 07:48, OPERATOR CONFIRMED overlay works after relaunch+OBS source refresh). This build contains the FULL perf set: #2 events.log 25MB cap, #3 chat-backfill 5→30s backoff, #4 day-scoped RoutineTable (+day-selector dropdown in column-title header), #5 overlay animation gating (content-visibility REMOVED — it was a wrong theory; the real blank-overlay bug was a stray backtick in a CSS comment inside the buildOverlayHTML template literal, now fixed), ⋯ row action menu, drive-pill cutoff fix, chat sticky-scroll, ⋯-button cutoff fix, carry-over A featureCard THAT-WAS fix. Backups on DART: app.asar.bak.20260516-backtick (132,680,553 blank build), .20260516-cvfix, .20260516-cpufix (132,523,871 = last pre-#4/#5 build, clean overlay rollback-of-last-resort), .20260515-perfbatch. NOTHING committed/pushed — all uncommitted on feat/ui-redesign-pass1.**

**OUTSTANDING (all gated on operator, NOT started — fresh session must NOT auto-start these, wait for instruction):**
1. **Carry-over B — R222/R223 photo DB reassignment (verified NOT done).** comp 7f796653-9e5a-4652-8968-21b7d18320fc. 43 photos currently on R222 pkg c40d0848 (entry f4f819cf) with media_photos.captured_at >= 2026-05-15T20:23:02.463Z must move to R223 pkg d9961024 (entry 17a61249, currently 0 photos). media_photos keys on media_package_id + captured_at (no entry_id col). UPDATE media_photos SET media_package_id='d9961024-e887-45ed-893a-0983b31db0b4' WHERE media_package_id='c40d0848-ee7d-430b-afff-3c0f12645c02' AND captured_at >= '2026-05-15T20:23:02.463Z' AND deleted_at IS NULL — targeted, needs per-action confirm. THEN DART compsync-state.json patch (app CLOSED, no-BOM [System.IO.File]::WriteAllText UTF8Encoding($false)): R222.recordingStoppedAt=2026-05-15T20:23:02.463Z, R223.recordingStartedAt=20:23:02.463Z, R223.recordingStoppedAt=20:26:05.394Z, R223.status=recorded. Split videos already on R2. Judge J1/J2/J3 commentary still post-show. ~1.26GB leftover recovery files in DART routine-222 folder = post-show cleanup.
2. **Perf #1 audio-meter — DECISION NEEDED.** wsHub.ts already throttles WS audio rebroadcast to 5Hz (AUDIO_BROADCAST_THROTTLE=200). INBOX premise stale for WS path. Remaining cost = OBS InputVolumeMeters parsed 30-60/s upstream in obs.ts. Operator must decide: do the OBS-ingest coalesce or leave #1 as-is.
3. **Clear-media row action (operator-requested, investigated only).** "Clear a routine's media + reset date/timestamp, never delete anywhere — archive." CompPortal has NO clear/archive-to-archive feature: media router = getPackageByEntry,...,deletePhoto,updateVideoUrl,...,updatePackageStatus,generate*Thumbnail* (no clear/archive/reset proc; no plugin endpoint; no CD-UI action). What EXISTS: reassign-routine/reassign-photos/reassign-videos (move EVERYTHING between routines, both entryIds required, can't clear-to-nowhere) + media_photos/media_packages.deleted_at soft-delete (recoverable). Build = small: new CompPortal endpoint (set deleted_at on pkg+photos, null video fields) + CSE local archive (state.setTakeArchived/_archive/vN exist) + state reset. Cross-repo.
4. **R2 archive-on-overwrite (data-safety, confirmed gap).** CompPortal src/lib/r2.ts signGuardedUpload (the CSE re-record path): on force=true it only HeadObject-logs the old object then signs a PUT to the same key → R2 overwrites in place, old bytes UNRECOVERABLE. Bucket has no versioning. copyInR2() exists but signGuardedUpload never calls it. So "R2 never deletes on overwrite" is FALSE for re-record/Replace. Fix (CompPortal-only, Vercel deploy not asar): in signGuardedUpload when overwroteExisting, copyInR2(key → <key>.archived/<ts>) before returning signed URL. ~30-45min.
5. **Commit/push** — operator has not asked; all work uncommitted on feat/ui-redesign-pass1. Do NOT commit without explicit go.

**KEY LESSONS THIS SESSION (saved to memory):** [[feedback_no_live_swap_on_unverified_theory]] — never build+swap a fix to live DART on a subagent's theory without the actual runtime error; [[reference_overlay_template_literal_backticks]] — overlay.ts buildOverlayHTML is ONE template literal (1373-5231), never put backticks/${ in CSS comments inside it; [[feedback_no_askuserquestion_tool]] — AskUserQuestion UI doesn't work for this user, ask in plain text one question at a time.



**Status: 2026-05-15 19:50 EDT (fresh session — perf backlog prep). Operator directive: "don't swap but keep preparing" (UDC Cobourg live on DART). BUILT + LOCALLY VERIFIED, NOT SWAPPED. SWAPPED LIVE 20:35 EDT (132,523,871, backup app.asar.bak.20260515-perfbatch=132,520,728 prior tabs build). Operator relaunched. Follow-up cutoff fix: ⋯ action button was right-aligned into the overflow-x:hidden clip zone of the last column — reverted to justifyContent flex-start (RoutineTable.tsx). BACKTICK-FIX BUILD SWAPPED LIVE 2026-05-16 ~07:48 EDT. Live `app.asar` = 132,532,621 (byte-exact verified). Backup `app.asar.bak.20260516-backtick` = 132,680,553. TRUE ROOT CAUSE of the blank overlay (present since the 07:25 swap; the content-visibility theory was WRONG, strike 1 wasted): overlay.ts:1901 had a CSS comment with unescaped backticks `` `.starting-soon.visible .ss-gradient-bg` `` INSIDE the buildOverlayHTML template literal (1373→5231). The first backtick closed the literal early; following CSS parsed as JS → `ReferenceError: soon is not defined` (from `starting-soon`→`starting - soon`) → overlay HTTP route 500 → blank. Evidence: operator pasted the exact runtime ReferenceError + stack (buildOverlayHTML, index.js:8154). Fix: backticks→apostrophes in that comment + a rule comment "never backticks inside this template literal". Verified: zero backticks between 1373-5231, 48 balanced total, clean electron-vite build (unterminated literal would fail build). #4 day-scoping + #5 animation gating BOTH retained — the only defect was the comment backtick. PENDING operator confirm: overlay (Starting Soon) renders + CPU dropped. Prior (now historical, both superseded): Live `app.asar` = 132,680,553 (byte-exact verified post-swap). Backup `app.asar.bak.20260516-cvfix` = 132,681,220 (the blank-overlay build, rollback). REGRESSION HISTORY: 07:25 swap of 132,681,220 (CPU-fix build with #5 content-visibility) blanked the OBS overlay — root cause: `content-visibility:auto` on always-in-viewport full-bleed absolute `.starting-soon`/`.feature-card` never re-fires render-unlock under OBS CEF, painted only intrinsic-size skeleton. Class names matched (not a mismatch); animation gating was fine. Surgical fix: removed all 4 content-visibility/contain-intrinsic-size declarations from overlay.ts (.starting-soon + .feature-card base+visible); #5 animation gating + #4 day-scoping retained = CPU win preserved. Both Move-Items exit 0 (app closed). PENDING operator: (1) relaunch CSE; (2) refresh OBS overlay browser-source; (3) confirm overlay (Starting Soon) shows AND CPU dropped. Not yet observed/confirmed. Rollback target if still blank: app.asar.bak.20260516-cvfix. This staged build = the FULL set: #2 events.log cap, #3 chat backoff, ⋯ action menu, drive-pill cutoff fix, chat sticky-scroll, ⋯-button cutoff fix, **#5 overlay animation hygiene** (gates infinite anims on .feature-card/.starting-soon visible state + content-visibility + will-change removed + sparkles 14→7 — the obs-browser-page ~1.75-core hog; REQUIRES OBS browser-source refresh after swap), **#4 day-scoped RoutineTable** (renders only current day ~100-300 rows vs ~1450; reuses existing dayFilter store; cross-day search-jump preserved; session dividers + current-routine auto-scroll preserved; windowMode unchanged) + day-selector DROPDOWN in the column-title header row (orderedDays select + ‹ › steppers + AUTO follow-current). #4+#5 are the two structural CPU drivers; CPU stayed at 100 because only the disk/IPC batch was swapped at 20:35. After this swap: operator MUST refresh the OBS overlay browser source for #5 to take effect. Contains: (#2) events.log 25MB roll-over to events.log.1 (events.ts); (#3) chat-backfill 5s→30s exponential backoff, self-rescheduling setTimeout (chatBridge.ts); per-row action menu — all 7 inline row buttons collapsed into a single ⋯ trigger opening a floating `.row-action-menu` (RoutineTable.tsx; NoteEditor gained `triggerless`; Nudge right-click auto-toggle preserved as explicit "Toggle auto-encode/upload" menu item); drive-alert import-pill cutoff fix — slot now content-sized up to 560px cap, no more "…done 7:0" truncation (drive-alert.css); InlineChatStrip chat-history sticky-bottom scroll fix — was force-scrolling to newest every 5s poll so operator couldn't read old comments; now only auto-scrolls if already at bottom, `atBottomRef` + `onHistoryScroll` (OverlayControls.tsx); carry-over A featureCard THAT-WAS getPreviousRoutine fix (already in source, rides this build). Local headless verify on port 9223 (TEST2026/Pinnacle): `.row-action-menu` renders/opens, items status-gated correctly; screenshot sent to operator TG DM 7761116558 msg 3769. npm run build PASS, no TS errors. STILL PENDING (gated/not started): carry-over B R222/R223 photo reassignment (verified did NOT run — 43 photos on R222 pkg c40d0848 with captured_at>=2026-05-15T20:23:02.463Z must move to R223 pkg d9961024 which has 0; plus DART compsync-state.json patch, app-closed no-BOM); perf #1 (audio meter — WS already throttled 5Hz, remaining OBS-ingest scope needs operator decision); #4 RoutineTable day-scoping (not started); #5 overlay animation hygiene (not started); #6 = AUDIT PASS, no code (index.ts:199-202 → obs.ts:910 previewPaused guard confirmed). Next prep step needs operator go: scp asar to DART staging path. NOTHING committed/pushed.**

**Status: 2026-05-15 19:01 EDT (/fresh handoff from CompSyncElectronApp-2). Live UDC Cobourg 2026 show. Fresh session's ONLY task: implement INBOX.md perf backlog #1–#6 (operator-approved) — NOT during a live event; wait for quiet window + operator go. Two carry-over items also pending: (a) featureCard THAT-WAS fix (getPreviousRoutine) is in local source but NOT yet swapped to DART — needs package+stage+swap; (b) R222/R223 merged-recording photo DB reassignment — cron 1466ac62 was session-only and dies with the old session, so verify/redo: reassign media_photos for comp 7f796653-9e5a-4652-8968-21b7d18320fc currently on R222 (entry f4f819cf) with exif_ts >= 2026-05-15T20:23:02.463Z over to R223 (entry 17a61249); also patch DART compsync-state.json R222.recordingStoppedAt=20:23:02.463Z / R223.recordingStartedAt=20:23:02.463Z / R223.recordingStoppedAt=20:26:05.394Z / R223.status=recorded ONLY with app closed (no-BOM write). R222/R223 split videos already uploaded to R2 (packages c40d0848 / d9961024); judge J1/J2/J3 still post-show. Live DART app.asar = 132,520,728 (17:55 EDT "tabs" build). Full detail + protocol + API keys in INBOX.md perf entry and the prior status blocks below. Reason for refresh: move perf work to a clean session away from the long live-show context.**

**Status: 2026-05-15 14:48 EDT. HEVC Settings ASAR built and staged for DART, not swapped. `src/renderer/components/Settings.tsx` now exposes Tablet Display → Encoder with `H.264 (OpenH264)` and `H.265/HEVC (NVENC)`. `resources/defaults/compsync-media-settings.json` now defaults `wifiDisplay.encoder` to `openh264`. `resources/wifi-display-server.exe` was replaced locally with the fixed HEVC access-unit/AUD build from WifiDisplay (`877997cf727e97a7545b3c295a774c0f5461fbcaa39450d79ead13a62b135020`) so future packages include it. Build: `npx electron-vite build && npx electron-builder --win --dir` passed after bypassing `npm run dist` because this Ubuntu shell lacks `dotnet` for the `predist` WPD helper publish. Local ASAR hash: `e91dc117c2d4ab855f0674517401d2c1b1fd2c6feff67ebc6d53c9dd2d1d2d9a`. Staged on DART at `C:\CompSyncStaging\2026-05-15-hevc-settings-asar\app.asar`; staged hash matches. DART live Program Files ASAR remains old hash `093c7b78b580b8644cb3e66acc7d38490dbd35a6e215b3e2dc534efb33d1430e`; no swap performed after operator said DO NOT SWAP. A backup of current live ASAR exists: `C:\Program Files\CompSync Media\resources\app.asar.bak.20260515-1448-hevc-settings`, same hash as live.**

**Status: 2026-05-14 21:49 EDT. CSE-side CompPortal overwrite guard UX deployed to DART. `src/main/services/upload.ts` now catches CompPortal `409 KEY_EXISTS` from `/api/plugin/upload-url`, prompts the operator for video collisions with existing size + lastModified formatted in America/New_York, and retries the same signing request with `force: true` when the operator chooses Replace. Cancel keeps the existing recording and aborts the upload job. The force decision is cached per `routineId:uploadRunId` so one intentional re-record does not prompt separately for performance/judge/keyframe signings. Current CSE late-insert uploads resolve synthetic IDs via `/api/plugin/late-insert-resolve` and then use the same `/api/plugin/upload-url` path, so this handler covers that flow too. Verification: `npm run build` passed; `npx electron-builder --win --dir` passed; staged ASAR was 132,504,505 bytes; live backup `app.asar.bak.20260514-key-exists-guard`; live `app.asar` is 132,504,505 bytes with LastWriteTime 2026-05-14 21:49:09 EDT; 4 CompSync Media processes responding.**

**Status: 2026-05-14 18:28 EDT. Chat admin visibility fix swapped live on DART. The taskbar-safe patch kept the rail above the Windows taskbar but pushed the `CHAT AS ADMIN` composer out of the visible flex area. `InlineChatStrip` now renders the admin composer directly below the `Live Chat` header, and the message feed/history is the shrinkable scroll area (`min-height: 0`). Verification: `npm run build` passed; `npx electron-builder --win --dir` passed; staged ASAR was 132,500,538 bytes; live backup `app.asar.bak.20260514-chat-admin-visible`; live `app.asar` is 132,500,538 bytes with LastWriteTime 2026-05-14 18:28:01 EDT; 4 CompSync Media processes responding.**

**Status: 2026-05-14 18:24 EDT. Taskbar-safe right rail patch swapped live on DART after operator screenshot showed the chat admin row under the Windows taskbar. `src/renderer/styles/show-rail.css` now reserves a 58px bottom safe zone inside `.show-rail` (`--windows-taskbar-safe-bottom`). Verification: `npm run build` passed; `npx electron-builder --win --dir` passed; staged ASAR was 132,500,423 bytes; live backup `app.asar.bak.20260514-taskbar-safe-rail`; live `app.asar` is 132,500,423 bytes with LastWriteTime 2026-05-14 18:24:54 EDT; 4 CompSync Media processes responding.**

**Status: 2026-05-14 18:20 EDT. Follow-up right-rail cut-off fix swapped live on DART. The first patch still showed a separate `FEATURE CARD` row and bottom live chat remained cut off. Second patch moved `UP NEXT` / `THAT WAS` into the `GRAPHICS` title bar itself, removing the standalone feature-card row entirely. Verification: `npm run build` passed; `npx electron-builder --win --dir` passed; staged ASAR was 132,500,330 bytes; live backup `app.asar.bak.20260514-feature-card-title`; live `app.asar` is 132,500,330 bytes with LastWriteTime 2026-05-14 18:20:34 EDT; 4 CompSync Media processes responding.**

**Status: 2026-05-14 18:17 EDT. Right-rail graphics/chat layout patch built and swapped live on DART. `UP NEXT` / `THAT WAS` feature-card controls moved into the top Graphics module beside Ticker / Starting Soon as compact buttons; the duplicate full-width feature-card row was removed from the Overlay & Lower Third block in `ShowControlRail`. Files changed: `src/renderer/components/OverlayControls.tsx`, `src/renderer/components/ShowControlRail.tsx`, `src/renderer/styles/overlay-controls.css`. Verification: `npm run build` passed; `npx electron-builder --win --dir` passed; staged `release/win-unpacked/resources/app.asar` to `C:\CompSync-staging\app.asar.new`; live backup `app.asar.bak.20260514-feature-card-chat`; live `app.asar` is 132,499,549 bytes with LastWriteTime 2026-05-14 18:17:16 EDT; 4 CompSync Media processes responding.**

**Status: 2026-05-12 01:37 EDT. Burlington cryo archive transfer DONE under pipeline task `task-1778550068006-c106` / `archive-udc-burlington-2026-dart-to-cryo`. Started 2026-05-11 21:46:23 EDT, finished 2026-05-12 01:32:22 EDT. Final summary `/mnt/firmament/CryoStorage/_transfer_logs/UDC_Burlington_2026_summary_20260511-214623.json` reports source_after = destination_after = 32,809 files / 436,868,649,223 bytes, `verified_counts_and_bytes=true`, `source_deleted=false`. Destination is `/mnt/firmament/CryoStorage/UDC Burlington 2026`; rclone log is `/mnt/firmament/CryoStorage/_transfer_logs/UDC_Burlington_2026_rclone_20260511-214623.log`. Duplicate pending task `task-1778550240857-ff42` remains disabled.**

**Status: 2026-05-12. Backup Media feature hardened in code. `src/main/services/backup.ts` now defaults to current-competition backup scope, preserves nested folder structure during recursive copy, verifies source/destination file presence and byte counts, and writes `.compsync-backup/backup-summary-*.json` manifest after verification. `BackupMedia.tsx` exposes Current competition vs All configured media roots and shows verified vs copied-but-unverified results. `npm run build` passed.**

**Status: 2026-05-11 12:22 EDT. Share-code input diagnostic asar swapped live on DART after operator reported empty-box typing still fails. New `app.asar` is 132,491,045 bytes, LastWriteTime 2026-05-11 12:22:51 EDT; backup of the previous share-code patch is `app.asar.bak.20260511-sharecode-input-pre-diagnostic` (132,490,210 bytes). 4 CompSync Media processes responding.**

Branch: `feat/ui-redesign-pass1` — uncommitted work spans Activity panel migration + Visual Editor sizing/off-screen + webm logo support.

## Last Session Summary
Iterated build9o → build9x on top of v2.8.0. Major work: integrated post-record audio audit into the unified Activity (Event Log) panel, fixed broken-link icon for video/webm logo overrides via `/element-asset` route, fixed off-by-one in `browseAsset()` IPC return type, and unblocked Visual Editor element placement at canvas corners + off-screen.

## What Changed (uncommitted on `feat/ui-redesign-pass1`)

### Share Code Input Focus Fix
- `src/renderer/components/LoadCompetition.tsx` — stopped mouse/click events inside the Load popover from bubbling to outside-close handlers, so typing in the Live share-code box is not interrupted. First fix failed operator's empty-box path.
- Diagnostic now live: share-code input logs `[share-code-input]` pointer/mouse/focus/key/change events with value lengths only. Waiting for operator to reproduce once, then read `C:\Users\User\AppData\Roaming\compsync-media\logs\main.log`.
- Verification before diagnostic: `npm run build` passed; disposable Electron/xvfb run dismissed startup overlays, opened Load, clicked the input, typed `abc-123`, and confirmed input value `ABC-123` with active element `INPUT`. Packaged with `npx electron-builder --win --dir`, staged to DART, swapped live, and verified 4 responding processes.

### Activity (Event Log) Panel
- `src/renderer/components/EventLogPanel.tsx` — added `HIDDEN_KINDS` set: 11 noisy event kinds filtered (`import.requested`, `import.match.summary`, `import.match.warning`, `recording.started`, `recording.stopped`, `encode.started`, `encode.completed`, `upload.started`, `upload.completed`, `chat.backfill.ok`, `chat.message.received`). Added 5 audio.audit.* formatters.
- `src/renderer/styles/event-log.css` — chips no-wrap (8.5px), tighter row padding, smaller text (line1 10.5px, line2 9.5px), errors+warnings get outer glow box-shadow.
- `src/renderer/styles/header.css` — `.topband-activity` capped at `max-height: 220px` with `overflow: hidden` + `min-height: 0` on child to fix infinite vertical stretch.
- `VISIBLE_BUCKETS` in EventLogPanel.tsx: removed `recording` and `chat` chips → 7 chips fit one row.

### Audio Audit → Event Log Migration
- `src/main/services/ffmpeg.ts` — added `events.emit('audio.audit.*')` calls alongside existing `sendToRenderer(IPC_CHANNELS.AUDIO_*)` for: `identicalTracks.warning`, `silence.warning`, `lowLoudness.warning`, `lowBitrate.warning`, and the `summary` (pass) event.
- `src/renderer/App.tsx` — removed `AudioAuditBanner` import + mount. Banner is gone; findings now flow into Activity panel only.

### Webm Video Logo Fix
- `src/main/services/overlay.ts`:
  - `<img>` brand surfaces (`#logoImg`, `#ltBrandImg`, `#ss-logo-img`, `#fc-brand-img`) get default `src="data:image/svg+xml;base64,PHN2Zy8+"` (no-op SVG) so they never show broken-image when src is empty.
  - `applyState` Flow A (`#logo` visibility): now flips `.visible` on when EITHER `o.logo.url` OR `o.logo.assetUrl` is set; null-guards `logoImg.src=` so it doesn't crash if `mountElementAsset` already replaced the `<img>` with a `<video>`.
  - `/brand-logo` HTTP route: if configured file is `mp4|webm|mov|m4v`, returns a 1×1 transparent PNG instead of streaming bytes (legacy `<img>` slots can't trip a broken-image icon when accidentally pointed at a video file).
  - `mountElementAsset`: video/img use `width: 100%; height: 100%; object-fit: cover; display: block` (changed from `maxWidth: 100% / contain`). Bounding box matches visible asset exactly.
  - Diagnostic console.log added in `mountElementAsset`: `[mountElementAsset] <key> assetUrl=... isVideo=true/false` — visible in iframe DevTools.
- `src/renderer/components/VisualEditor.tsx`:
  - **CRITICAL bug fix**: `browseAsset()` was treating `settingsBrowseFile` return as an array (`result[0]` indexed first character of the path string — `"D"` from `"D:\..."`). Now checks `typeof result === 'string'` and uses the whole string. THIS is what made the broken-link appear after picking a webm.
  - Drag clamps widened from `0..95` (X) / `0..98` (Y) to `-100..200` for both — element can be dragged completely off-screen.
  - Resize clamps widened to `2..200` (was `5..(100-pos.x)` etc.) — allows oversize beyond canvas.
  - Arrow-key nudge clamps matched to drag clamps.
  - "Reset position" button added in per-element properties panel — restores selected element's x/y/w/h from `DEFAULT_LAYOUT`. Hint text updated.
- `src/renderer/styles/visualEditor.css`:
  - Resize handles (`.ve-handle-right`, `.ve-handle-bottom`, `.ve-handle-bottom-right`) moved from `right: -8px` / `bottom: -8px` (outside element, clipped at canvas edge) to `right: 1px` / `bottom: 1px` (inside element).
  - `.ve-element.selected` outline switched from `outline-offset: 3px` (outside) to `outline-offset: -1px` (inset).

## Build Status
PASSING — last build9x, 132,489,458 bytes, swapped to live on DART at 14:50 EDT. TypeScript clean throughout the session.

## Live state on DART (per Phase 3 verify after build9x cutover)
- `app.asar` Length: 132,489,458 (matches local) · LastWriteTime: 2026-05-07 14:49:40 EDT
- 4× CompSync Media processes, all `Responding=True`
- **Backups (most recent first):**
  - `app.asar.bak.20260507-build9w-pre9x` — 132,488,671
  - `app.asar.bak.20260507-build9v-pre9w` — 132,488,409
  - `app.asar.bak.20260507-build9u-pre9v` — 132,488,409
  - `app.asar.bak.20260507-build9t-pre9u` — 132,487,837
  - `app.asar.bak.20260507-build9s-pre9t` — 132,487,855
  - `app.asar.bak.20260507-build9r-pre9s` — 132,498,447
  - older chain back to v2.7.0-stable

## Known Bugs & Issues
- **None outstanding from this session.** Webm logo override works end-to-end after the `browseAsset()` fix. Activity panel container constrained.
- Branding-side logo (`Settings → Logo Image → Browse`) uses the legacy `/brand-logo` route and was not extended for video — only the per-element Visual Editor `assetUrl` flow supports webm. If operator picks a webm via the Settings dialog, `/brand-logo` returns a transparent PNG (no broken icon) but no video plays; they should use Visual Editor → element → Asset override → Browse instead. Not flagged as a defect, just a known surface boundary.

## Incomplete Work
- **Diagnostic console.log left in** `src/main/services/overlay.ts:mountElementAsset`. Cheap no-op in production; can stay or be removed in next session.
- All build9 changes are uncommitted to git (HEAD still at `4f9ac8f bump 2.8.0`). Wrap-up commit in progress.

## Tests
- No formal test runs this session — all verification was operator-driven on live DART during Burlington comp.
- Manual verification done: Activity panel rows render (build9s+), webm logo loads after correct `assetUrl` save (build9u+), Visual Editor handles + off-screen drag (build9w+ / build9x).

## Next Steps (priority order)
1. **Wait for operator verification of build9x** — they were sizing/placing the video logo when session ended. If anything's off (cropping wrong, off-screen behaving weird, reset button not where expected), iterate.
2. **Consider unifying brand logo override** — operator originally expected `Settings → Logo Image` to also accept webm. Could either (a) extend that to support video (parallel pipeline to `/element-asset`), or (b) cross-link from Settings to Visual Editor with copy explaining where video goes. Not urgent.
3. **Migrate remaining banners/toasts to Activity panel** (operator's locked spec from 2026-05-05): `HardeningBanners` (10 alert kinds in App.tsx), `AutoToggleToast`, `ReconcileToast`, `MissingPhotosToast`, `OffsetConfirmToast`, `ImportBusyBanner`, `DriveAlert`. Interactive ones become inline action rows. Audio audit was the first surface migrated this session — pattern is established.
4. **Late Cut spec** still locked at `docs/plans/2026-05-04-late-cut.md`, NOT implemented.
5. **CSController HEVC decoder swap** still queued in `~/projects/CSController/INBOX.md` — production gate before flipping `settings.wifiDisplay.encoder = 'hevc-nvenc'`.

## Gotchas for Next Session
- `settingsBrowseFile` IPC returns `string | null` (single path), NOT `string[]`. Any new "browse file" caller in the renderer should `typeof result === 'string'` check, not `result[0]`. The bug we hit affected `VisualEditor.browseAsset()`; verify other callers (`src/preload/index.ts:61` is the contract).
- `/element-asset?key=<key>` is the per-element route (mp4/webm/mov/m4v MIME supported). `/brand-logo` is the legacy single-logo route (no video — returns transparent PNG for video extensions to avoid broken-icon).
- `mountElementAsset` REPLACES the inner `<img>` with a `<video>` (or another `<img>`) when `assetUrl` is set. Any code that grabs `document.getElementById('logoImg')` after that point may get null or a `<video>` — handle defensively.
- Activity panel's `.topband-activity` has hard `max-height: 220px` cap. If operator wants more visible rows, tune that constant.
- Drag clamps in Visual Editor allow `-100..200` percent — that means an element can be COMPLETELY off-screen. Default Layout is the only reset path (per-element Reset button restores from `DEFAULT_LAYOUT`).
- This session did NOT touch upload/queue/encoder/recording/state/SD ingest/CompPortal sync. Burlington comp was running live throughout — only render-tier changes.

## Files Touched This Session
**Modified (uncommitted):**
- `CURRENT_WORK.md` — this file (overwritten)
- `INBOX.md`
- `src/main/services/ffmpeg.ts` — audio.audit.* events
- `src/main/services/overlay.ts` — mountElementAsset, /brand-logo, /element-asset wiring, default img srcs, diagnostic log
- `src/renderer/App.tsx` — removed AudioAuditBanner mount + import
- `src/renderer/components/EventLogPanel.tsx` — HIDDEN_KINDS, audio.audit.* formatters, removed 2 chips
- `src/renderer/components/VisualEditor.tsx` — browseAsset string fix, drag clamps -100..200, Reset position button
- `src/renderer/styles/event-log.css` — chip + row sizing, error/warn glow
- `src/renderer/styles/header.css` — .topband-activity height cap
- `src/renderer/styles/visualEditor.css` — handle positions, outline-offset

## Memory rules to honor
1. `feedback_never_kill_or_start_user_apps.md` — operator owns CSE close + start (broken twice this session — operator explicitly said "you close it" / "yes" to authorize)
2. `feedback_asar_swap_explicit_only.md` — every swap is its own gated action
3. `feedback_partial_is_not_done.md` — ✅ means END-TO-END
4. `feedback_use_log_server_not_ssh.md` — query Supabase `machine_logs` for DART logs (used twice this session to confirm webm/asset state)
5. `feedback_no_chat_messages.md`
6. `feedback_just_execute.md` — read-only ops don't gate
7. **NEVER auto-code from CURRENT_WORK or this transcript.** Wait for operator instruction.

## Build/deploy reference
- Live asar path on DART: `C:\Program Files\CompSync Media\resources\app.asar`
- Staging path on DART: `C:\CompSync-staging\app.asar.new`
- Local staging: `release/win-unpacked/resources/app.asar` (electron-builder output)
- Cutover protocol: `docs/runbooks/asar-swap-protocol.md`. Single-line per Move-Item via separate ssh invocations.
- Build cmd: `npm run build && npx electron-builder --win --dir`

## Pre-existing Late Cut spec (locked, not implemented)
`docs/plans/2026-05-04-late-cut.md` — 114 lines, 12 sections. Recovery flow for missed routine boundaries. Verified at session start: 0 references in `src/`, no commits in git history.
